{
  "name": "@doxynix/shared",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "exports": "./src/index.ts",
  "scripts": {
    "format": "biome check --write .",
    "format:check": "biome check .",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "type-check": "tsc -b",
    "validate": "biome check . && oxlint && bun type-check"
  },
  "dependencies": {
    "zod": "catalog:"
  },
  "devDependencies": {
    "@doxynix/config": "workspace:*"
  }
}
